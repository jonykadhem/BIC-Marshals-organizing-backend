const express = require("express")

const Events = require('../models/events')


const create = async (req, res) => {
    try {

        
    } catch (error) {
        console.log(error)
    }
}

module.exports = {

    create,
}